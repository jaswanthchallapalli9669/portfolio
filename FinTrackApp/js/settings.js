document.addEventListener('DOMContentLoaded',()=>{if(document.body.dataset.page!=='settings')return;const data=getData(),form=document.getElementById('profileForm');form.name.value=data.profile.name;form.email.value=data.profile.email;form.onsubmit=(event)=>{event.preventDefault();const values=Object.fromEntries(new FormData(form));updateData((next)=>next.profile=values);showMessage('Profile saved.');document.getElementById('profileName').textContent=values.name};document.getElementById('settingsTheme').onclick=()=>{updateData((next)=>next.settings.theme=document.body.classList.contains('dark')?'light':'dark');document.body.classList.toggle('dark')};document.getElementById('exportCsv').onclick=exportCsv;document.getElementById('exportJson').onclick=exportJson;document.getElementById('importJson').onchange=importJson;document.getElementById('clearData').onclick=()=>{if(confirm('Clear all transactions, budgets, and goals? This cannot be undone.')){localStorage.removeItem(STORAGE_KEY);location.href='index.html'}}});function download(content,name,type){const link=document.createElement('a');link.href=URL.createObjectURL(new Blob([content],{type}));link.download=name;link.click();URL.revokeObjectURL(link.href)}function exportCsv(){const rows=getData().transactions,header='Type,Amount,Description,Category,Date,Payment method,Notes';const csv=[header,...rows.map((item)=>[item.type,item.amount,item.description,item.category,item.date,item.paymentMethod,item.notes].map((value)=>`"${String(value).replaceAll('"','""')}"`).join(','))].join('\n');download(csv,'fintrack-transactions.csv','text/csv') }function exportJson(){download(JSON.stringify(getData(),null,2),'fintrack-backup.json','application/json')}function importJson(event){const file=event.target.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const backup=JSON.parse(reader.result);if(!backup.transactions||!backup.budgets||!backup.goals)throw new Error();saveData(backup);showMessage('Backup imported. Refreshing...');setTimeout(()=>location.reload(),600)}catch{showMessage('That file is not a valid FinTrack backup.')}};reader.readAsText(file)}function showMessage(message){document.getElementById('settingsMessage').textContent=message}
